<% content_for :title, "Clientes | BiDesk" %>
<% ui_page_shell %>
<% ui_page_header(
  breadcrumbs: [{ label: "Dashboard", path: root_path }, { label: "Clientes" }],
  title: "Clientes",
  subtitle: "Gerencie e visualize todos os seus clientes",
  icon: :clientes
) %>

<div class="mb-5">
  <span class="inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-sm font-medium text-emerald-800">
    <svg class="h-4 w-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
    </svg>
    <% if params[:busca].present? || clientes_filtros_ativos? %>
      <%= pluralize(@clientes.size, "cliente encontrado", "clientes encontrados") %>
    <% else %>
      Total de <%= pluralize(@clientes.size, "cliente", "clientes") %>
    <% end %>
  </span>
</div>

<%= render layout: "shared/page_toolbar" do %>
  <div class="<%= ui_toolbar_layout_class %>" data-filter-toolbar>
    <%= form_with url: clientes_path, method: :get, local: true, html: { class: ui_toolbar_form_class, data: { filter_form: true } } do |f| %>
      <div class="<%= ui_toolbar_search_wrapper_class %>">
        <span class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-4 text-gray-400" aria-hidden="true">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </span>
        <%= f.text_field :busca,
            value: params[:busca],
            placeholder: "Buscar por nome, telefone ou email...",
            class: ui_search_input_class %>
        <%= f.submit "Buscar", class: "sr-only" %>
      </div>

      <%= render "shared/filter_details",
          active: clientes_filtros_ativos?,
          show_clear: params[:busca].present? || clientes_filtros_ativos?,
          clear_path: clientes_path,
          clear_label: (params[:busca].present? ? "Limpar filtros e busca" : "Limpar filtros") do %>
        <%= render "shared/filter_field", label: "Ordenar por" do %>
          <div class="space-y-2.5 text-sm text-gray-700">
            <label class="flex cursor-pointer items-center gap-3 rounded-lg px-2 py-2 hover:bg-gray-50">
              <%= radio_button_tag :ordenar, "recentes", params[:ordenar].blank? || params[:ordenar] == "recentes", class: "text-emerald-600 focus:ring-emerald-500", onchange: "this.form.requestSubmit()" %>
              <span>Mais recentes</span>
            </label>
            <label class="flex cursor-pointer items-center gap-3 rounded-lg px-2 py-2 hover:bg-gray-50">
              <%= radio_button_tag :ordenar, "nome_asc", params[:ordenar] == "nome_asc", class: "text-emerald-600 focus:ring-emerald-500", onchange: "this.form.requestSubmit()" %>
              <span>Nome (A–Z)</span>
            </label>
            <label class="flex cursor-pointer items-center gap-3 rounded-lg px-2 py-2 hover:bg-gray-50">
              <%= radio_button_tag :ordenar, "nome_desc", params[:ordenar] == "nome_desc", class: "text-emerald-600 focus:ring-emerald-500", onchange: "this.form.requestSubmit()" %>
              <span>Nome (Z–A)</span>
            </label>
          </div>
        <% end %>
        <div class="border-t border-gray-100 pt-1">
          <label class="flex cursor-pointer items-center gap-3 rounded-lg px-2 py-2 text-sm text-gray-700 hover:bg-gray-50">
            <%= check_box_tag :com_email, "1", params[:com_email] == "1", class: "rounded border-gray-300 text-emerald-600 focus:ring-emerald-500", onchange: "this.form.requestSubmit()" %>
            <span>Apenas com e-mail cadastrado</span>
          </label>
        </div>
      <% end %>
    <% end %>

    <div class="<%= ui_toolbar_actions_class %>">
      <%= link_to clientes_path(clientes_export_params.merge(format: :csv)),
          class: "inline-flex w-full items-center justify-center gap-2 rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm font-medium text-emerald-800 shadow-sm transition hover:border-emerald-300 hover:bg-emerald-50/50 focus:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 sm:w-auto",
          title: "Baixar lista em CSV" do %>
        <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
        </svg>
        Exportar
      <% end %>

      <%= ui_modal_link new_cliente_path, class: "#{ui_btn_primary_class} w-full sm:w-auto justify-center" do %>
        <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
        Novo Cliente
      <% end %>
    </div>
  </div>
<% end %>

<div class="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm">
  <div class="overflow-x-auto">
    <table class="min-w-full">
      <thead>
        <tr class="border-b border-gray-200">
          <th scope="col" class="<%= ui_table_th_class %>">Nome</th>
          <th scope="col" class="<%= ui_table_th_class %>">Telefone</th>
          <th scope="col" class="<%= ui_table_th_class %>">Email</th>
          <th scope="col" class="<%= ui_table_th_class %>">Ações</th>
        </tr>
      </thead>
      <tbody id="clientes_tbody" class="divide-y divide-gray-100">
        <%= render "tbody", clientes: @clientes %>
      </tbody>
    </table>
  </div>
</div>
