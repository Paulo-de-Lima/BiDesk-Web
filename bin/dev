#!/usr/bin/env bash
set -e

cd "$(dirname "$0")/.."

if command -v foreman >/dev/null 2>&1; then
  exec foreman start -f Procfile.dev "$@"
fi

echo "foreman não encontrado; iniciando Rails + Tailwind em paralelo…"
trap 'kill 0' INT TERM
bin/rails tailwindcss:watch &
bin/rails server "$@"
